EVENT_DB_PATH=./macro_maintainer.sqlite3
MEM0_ENABLED=true

# Maintenance recency window (CLI recency-window / recency-audit / list-events --maintenance-window)
# MAINTENANCE_PAST_HOURS=72
# MAINTENANCE_FUTURE_DAYS=7

# Optional mem0 — in-process, no Docker (local Chroma + mem0_history.db)
# pip install -e ".[mem0]"
# MEM0_ENABLED=true
# MEM0_DUP_THRESHOLD=0.85
# MEM0_TEXT_CONFIRM_RATIO=0.75
# MEM0_TTL_DAYS=30
# DASHSCOPE_API_KEY=   # 必填（Embedding）；复制为 .env，勿提交 Git
# DEEPSEEK_API_KEY=    # 仅 MEM0_INFER_ON_ADD=true 时需要
# MEM0_VECTOR_STORE_PATH=./mem0_chroma
# MEM0_HISTORY_DB_PATH=./mem0_history.db
# MEM0_CONFIG_PATH=./configs/mem0.deepseek-qwen-chroma.json
